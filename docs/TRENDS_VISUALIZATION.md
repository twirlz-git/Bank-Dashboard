# Визуализация трендов - Документация

## Обзор

В ветке `feature/trends-mode-search-based` добавлены полноценные возможности визуализации трендов и работы с реальными данными.

## Новые модули

### 1. `modules/chart_generator.py`

**Назначение:** Генерация интерактивных графиков для анализа трендов и сравнения продуктов.

**Основные возможности:**
- `generate_timeline_chart()` - линейный график динамики изменений
- `generate_comparison_chart()` - столбчатая диаграмма сравнения
- `generate_trend_analysis_chart()` - детальный анализ с гистограммой изменений
- `generate_multiple_banks_comparison()` - сравнение нескольких банков

**Технологии:** Plotly (JavaScript-based интерактивные графики)

**Пример использования:**
```python
from modules.chart_generator import ChartGenerator

chart_gen = ChartGenerator()
fig = chart_gen.generate_timeline_chart(timeline_data, title="Динамика ставки")

# Для Streamlit
import streamlit as st
st.plotly_chart(fig, use_container_width=True)

# Для экспорта в PNG
fig.write_image("chart.png")
```

### 2. `modules/historical_generator.py`

**Назначение:** Генерация исторических данных на основе реальных JSON-файлов.

**Как работает:**
1. Загружает текущие данные продуктов из `configs/bank_data/`
2. Извлекает текущую процентную ставку
3. Симулирует исторические изменения с реалистичными флуктуациями
4. Создает временной ряд с высокой достоверностью (0.85)

**Преимущества:**
- Использует **реальные данные** из JSON-файлов
- Генерирует реалистичные тренды (постепенное снижение)
- Добавляет случайные флуктуации для естественности
- Не требует хранения исторических данных в БД

**Пример:**
```python
from modules.historical_generator import HistoricalDataGenerator

hist_gen = HistoricalDataGenerator()
timeline = hist_gen.generate_historical_timeline(
    bank="Альфа",
    product_type="credit_card",
    time_period="last_6_months"
)
# Возвращает timeline с реальными данными
```

## Обновленные модули

### 1. `modules/report_generator.py`

**Изменения:**
- Интеграция `ChartGenerator`
- Автоматическое встраивание графиков в XLSX-отчеты
- Дополнительные вкладки в Excel:
  - **Графики** - визуализация трендов
  - **Статистика** - числовые показатели
  - **Выводы** - текстовый анализ

**Новые возможности:**
- Экспорт графиков в PNG и вставка в Excel
- Автоматическое масштабирование изображений
- Улучшенное форматирование таблиц

### 2. `modules/trends_analyzer.py`

**Изменения:**
- Интеграция `HistoricalDataGenerator`
- Приоритет реальных данных над mock-данными
- Добавлено поле `data_source` в результат:
  - `"real_data_based"` - генерация на основе реальных файлов
  - `"web_search"` - данные из web-поиска
  - `"mock"` - тестовые данные

### 3. `app/main.py`

**Новые возможности UI:**

#### Trends Mode:
- ✅ Интерактивный линейный график динамики
- ✅ Детальный анализ с гистограммой изменений
- ✅ Статистические метрики (st.metric)
- ✅ Бэйдж источника данных (реальные/mock)

#### Urgent Mode:
- ✅ График сравнения продуктов

## Зависимости

Добавлено в `requirements.txt`:
```
kaleido>=0.2.1  # Экспорт Plotly графиков в PNG/JPEG
```

## Установка и запуск

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск приложения
streamlit run run.py
```

## Примеры использования

### Анализ трендов в Streamlit

1. Откройте приложение
2. Выберите "📈 Анализ трендов"
3. Выберите:
   - Банк (например, "Альфа")
   - Тип продукта (например, "Кредитная карта")
   - Период (например, "Последние 6 месяцев")
4. Нажмите "📊 Анализировать тренды"

**Результат:**
- ✅ Бэйдж источника данных (зеленый = реальные, желтый = mock)
- 📈 Интерактивный график динамики с hover-подсказками
- 🗓️ Детальный анализ с гистограммой изменений
- 📊 4 метрики со статистикой
- 📋 Таблица изменений
- 📥 Кнопка экспорта в XLSX

### Экспорт в XLSX

XLSX-отчет содержит:

**Вкладка "Тренды":**
- Информация о банке, продукте, периоде
- Таблица с временным рядом

**Вкладка "Статистика":**
- 10 числовых показателей

**Вкладка "Выводы":**
- Полный текстовый анализ

**Вкладка "Графики":**
- 2 встроенных PNG-изображения графиков

## Возможности

### Реальные данные
✅ Анализ основан на реальных JSON-файлах из `configs/bank_data/`  
✅ Высокая достоверность (85%)  
✅ Реалистичные тренды  

### Визуализация
✅ Интерактивные графики в UI  
✅ Встроенные графики в Excel  
✅ Множественные виды графиков  

### Аналитика
✅ Статистические метрики  
✅ Анализ тренда  
✅ Интерпретация результатов  

## Отличия от master

Ветка `master` **не затронута**. Все изменения сделаны только в `feature/trends-mode-search-based`:

**Добавленные файлы:**
- `modules/chart_generator.py`
- `modules/historical_generator.py`
- `docs/TRENDS_VISUALIZATION.md`

**Обновленные файлы:**
- `modules/report_generator.py`
- `modules/trends_analyzer.py`
- `modules/__init__.py`
- `app/main.py`
- `requirements.txt`

## Будущие улучшения

### Краткосрочные:
- [ ] Интеграция реального web-search
- [ ] Сравнение нескольких банков на одном графике
- [ ] Экспорт графиков в SVG/HTML

### Долгосрочные:
- [ ] Хранение реальной истории в БД
- [ ] Машинное обучение для предсказания трендов
- [ ] Автоматический сбор данных через web scraping

## Контакты

Для вопросов и предложений создайте Issue в репозитории.
